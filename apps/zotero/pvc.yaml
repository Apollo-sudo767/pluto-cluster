apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zotero-data
  namespace: productivity
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
