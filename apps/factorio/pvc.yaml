apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: factorio-data
  namespace: games
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 10Gi
